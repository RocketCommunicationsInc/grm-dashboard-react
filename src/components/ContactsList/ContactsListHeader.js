import './ContactsListHeader.scss';

const ContactsHeader = ({ isOpen, handleAction }) => (
  <header className='Contacts-header'>
    <h2>[State] Contacts 00:00 - 24:00</h2>
  </header>
);

export default ContactsHeader;
